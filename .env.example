# ============================================
# Quote Agent - Environment Variables
# ============================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# ============================================

# --------------------------------------------
# OpenAI API Configuration (REQUIRED)
# --------------------------------------------
# Your OpenAI API key for Mastra AI agents
# Get your key from: https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here

# --------------------------------------------
# Convex Configuration (OPTIONAL in dev)
# --------------------------------------------
# Convex deployment URL - auto-detected in development
# Required for production deployments
VITE_CONVEX_DEPLOYMENT=

# --------------------------------------------
# Notes:
# --------------------------------------------
# - VITE_ prefix is required for Vite to expose variables to the client
# - For server-side only variables (Convex actions), use without VITE_ prefix
# - The OPENAI_API_KEY is used in Convex actions (server-side)
# --------------------------------------------

# Convex server-side API key (for Convex actions)
OPENAI_API_KEY=sk-your-openai-api-key-here
